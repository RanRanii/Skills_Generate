# Experimental setup

We train the classifier for 10 epochs with a learning rate of 0.001. Model selection uses the validation score. We report macro-F1 on the held-out test set. The released result is 0.820.

